# Public CDP project identifier. Never put API key or Wallet Secret values here.
VITE_CDP_PROJECT_ID=your-cdp-project-id

# Optional same-origin API prefix. Leave empty when Vite proxies the local API.
VITE_API_BASE_URL=
